name: GitHub Build
on: [push]
jobs:
  SetUpEnvironment:
    runs-on: ubuntu-latest
    steps:
      - name: Install Java
        run: sudo apt install openjdk-21-jdk
      - name: Install Maven
        run: sudo apt install maven -y
